#include <cstdio>
#include "Car.h"
#include "SportsCar.h"
#include "Rectengle"
#include "MyHome"

int main() {
	Car myCar(50, "K3", 4);
	Car yourCar(100, "K5", 3);
	myCar.display();
	yourCar.display();
	myCar.whereAmI();
	yourCar.whereAmI();

	SporsCar scar;
	scar.speedUp();

}

Rectengle r(10, 20);
double perimeter = r.getPerimeter();
std::cout << "Rectengle 1:" << std::endl;
std::cout << "Area: " << r.getArea() << std::endl;
std::cout << "Perimeter:" << perimeter << std::endl;
std::cout << "is square?" << std::boolalpha << r.isSqare std::endl;

SmartHome myHome("¹Ú¼öÂù, 23, 40, true");

MusicStreamingService myService("Spotify");
myService.addMusic("Vive, ")
myService.addMusic("Vive, ")
myService.addMusic("Vive, ")

string music_title;
cout << "Enter the Music Title : ";
cin >> music_title;
Music* result = myService.searchBytitle(Musictitle)

String artist_name;
vector<Music*>

if (artistResult.size() > 0) {
	cout << "Found" << artistResult.size() << 
}